//! Liquid template helpers (re-exported from the `app` crate for cli-kit consumers).

pub use app::utilities::liquid::LiquidError;
pub use app::{recursive_liquid_template_copy, render_liquid_template};
